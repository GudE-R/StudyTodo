12/14
今日はReacyNative　Expoを導入して、モバイル版を構築した
が、AIとモバイルアプリに対する解釈の違いがあり、理想とは大きく離れた、モバイルアプリができ上ってしまった
今後はAIに対する要望を明確にしなければいけないと反省した
おおきな計画の前にはPlanだけでは足りないのかもしれない
機能要件を部分的に作成してもらい、ユーザーとエージェントの解釈に齟齬がないかたしかめながら解発することが
今後のバイ部コーディングをおこなう上で心がけるポイントだと考えた
ひとまず今日モバイルアプリ版のレイアウトを再構築した
今後テストをして旧Web版に近づけていく予定である

12/15
モバイル版の実装がうまくいかない原因がWeb版のコードの大きな変更にあったことが分かった
Gitの機能を使いWeb版のみ昨日のコードを復元した
これでモバイル版の実装も問題なく進めることができると考える

12/27
PCが壊れてしまったため、修理に出した
修理中の間は別のPCを使用して開発を続けていく
Linux mintに移行してみた
外付けSSDケースを購入して、SSD2から開発途中のデータにアクセスすることができた
Githubからもダウンロードできたが、最新のものはプッシュされていなかったため、SSD2のデータから開発を続けることにした

12/28
Mobile版のクラウド同期機能の実装が完了した
Web版とのデータ構造の違い（カラム不足）によりSupabase同期エラーが発生していたが、スキーマを更新して解消した
また、Mobile版のID生成がタイムスタンプなのが原因でUUIDエラーが出ていたため、UUID生成ライブラリを導入して修正した
これでWebとMobileで完全にデータ同期ができるようになった
そろそろマーケティングの勉強をしようと思う
アプリ販売の手順を調べる

12/30
モバイルアプリで「RangeError: Invalid time value」エラーが発生していたため、日付処理のバリデーションを追加した
複数のコンポーネント（Header, MobileCalendar, MobileTodoList等）で無効な日付に対してtoISOString()やgetDate()を呼び出していたのが原因だった
isNaN(date.getTime())チェックを追加し、無効な場合は現在時刻にフォールバックするよう修正

また、同じ名前のファイルが複数フォルダに存在していた問題を解消するためリファクタリングを実施した
- home/フォルダ: MobileCalendar→HomeCalendar, MobileDaySchedule→HomeDaySchedule, MobileTodoList→HomeTodoList
- widgets/フォルダ: MobileCalendar→CalendarMiniWidget, MobileDaySchedule→DayScheduleMiniWidget, MobileTodoList→TodoListMiniWidget  
- ui/フォルダ: Header→SimpleHeader

これでファイル名の混乱が解消され、デバッグやメンテナンスがしやすくなった

その他の改善点:
- デイスケジュールをFlatListからSectionListに移行し、日付ヘッダーをスティッキー表示（上部固定）にした
- 23:30から翌日0:00へのスクロールがスムーズになり、日付の境界付近での操作性が向上した
- タイムラインの30分ラインを実線（HH:00）と破線（HH:30）で視覚的に区別できるようにした
- 日付ヘッダーと0:00の時刻表示が重なる問題を修正した

同期機能の堅牢化:
- useMobileSync と useMobileRealtimeSync の日付比較ロジックにバリデーションを追加
- mapper.ts と SQLiteRepository.ts で不正な日付データを安全に処理できるよう改善

12/31〜1/1
ドキュメント整理と自動スケジュール連携機能の実装を行った。

docs/04_FUTURE.md の末尾に追記されていた未整理の項目を、「リファクタリング / 技術的負債」「機能アイデア」「今後の改善案」の各セクションに分類・統合した。

自動スケジュール連携機能:
- 時間指定付きTodoがスケジュールビューに自動反映される機能を実装
- packages/shared に getTodoScheduleRange() ユーティリティを作成し、Web/Mobileで共通のロジックを使用できるようにした
- 開始時間は dueTime ("HH:mm") を最優先、なければ dueDate の時間部分
- 終了時間/期間は endTime > estimatedDuration > デフォルト30分 の順で決定
- Web版(DaySchedule.tsx)とMobile版(DayScheduleWidget.tsx, DayScheduleMiniWidget.tsx)を共通ロジックに対応

しかし、Mobile版のSQLiteRepositoryにdueTimeとendTimeカラムが存在しなかったため、終了時間が保存されないバグがあった。
カラム追加とINSERT/UPDATEクエリの修正を行った際、fromDB関数の日付解析ロジックにバグを導入してしまい、
Todoやセッションのデータ読み込みが壊れる問題を引き起こした。
ISO形式("T"を含む長い文字列)のみDateに変換するよう修正して対応した。

共有コードの大幅リファクタリング（別途実施）:
- mapper, syncCore, date utils を packages/shared に統合
- Mobile版のテストフレームワークをJestからVitestに移行

開発方針の見直し (2026-01-01):
Web版の先行リリースを目指す「プラン1」を採用した。
同時開発による複雑性とデバッグの難しさが課題として浮き彫りになったため、まずはWeb版を「広告モデル」を含めて完成・リリースすることに集中する。
当初予定していたStripe決済機能の搭載は見送り、代わりにWeb広告を搭載する。
Proプラン（広告OFF、制限解除）の作成は、モバイル版が完成し、リリース後に問題がないことが確認された段階で確認する。

UI/UXの大幅な刷新 (2026-01-01):
Web先行開発の決定を受け、PC環境での生産性を最大化するためのUI改善を一気に実施。
1. Todo入力の簡素化
   - 作成モーダルの入力を「1行目タイトル、2行目以降メモ」という自由記述形式に統合。
   - カテゴリ選択時にタイトルを自動補完する仕組みを入れ、入力の摩擦を極限まで減らした。
2. Todo詳細画面の「詳細兼編集」モーダル化
   - 作成時と同じ入力フィールで詳細画面から直接修正・保存できるようになった。
3. カレンダーの視認性向上
   - 学習実績（ヒートマップ）に加え、その日のタスク数（ドット）を可視化。
   - 未来の忙しさと、過去の達成度を一目で把握できるダッシュボードとしての機能を強化。
4. テーマ管理と広告導線の整理
   - ダークモード反映の不具合をCSS変数による管理へ移行することで根本解決。
   - 使用ガイドをヘッダーへ移動し、設定画面を「データ管理」に特化させた。
5. フィードバック収集基盤の実装
   - アプリ内モーダルでユーザーの声を直接収集できる仕組みを構築。
   - オフライン対応（送信失敗時の自動キューイング）を備え、Supabaseとの同期も完了。
6. 収益化に向けた広告基盤の搭載
   - Google AdSense互換の広告ユニットをヘッダーに配置。
   - 環境変数一つでプレースホルダーから実広告に切り替え可能な本番仕様のコンポーネントを導入。

1/1 (夜)
Webアプリケーション全体の多言語化（Internationalization）が完了した。
当初は日本語のみだったが、グローバル展開を見据えて英語、ドイツ語、フランス語、スペイン語の計5言語をサポート。

実装のポイント:
- **共有リソースの設計**: 翻訳JSONを `@pomarc/shared` に集約し、Web/Mobile間での100%再利用を実現。
- **next-intl の導入**: Next.js App Router に完全準拠したロケールルーティング（`/ja`, `/en` 等）を構築。
- **全コンポーネントの国際化**:
  - カレンダー上の曜日表記や日付フォーマットをロケールに合わせて動的に最適化。
  - 作成・詳細モーダル、カテゴリ・SRSエディタ、設定、認証画面の全UI文字列を翻訳。
- **トラブルシューティング**: 
  - 実装直後に「next-intl config file not found」エラーが発生したが、`next.config.ts` で `createNextIntlPlugin` が未設定だったことを特定し修正。
  - ブラウザ検証を行い、OSの言語設定やURLパスに応じてシームレスに表示が切り替わることを確認。

これでWeb版は「世界中で使える広告付き生産性ツール」としての要件を技術的に満たした。
新年早々、非常に大きな基盤を整えることができた。

あけましておめでとう
新年から開発頑張った
Webリリースまであと少し
これからも頑張っていこう


1/5
統計ダッシュボードとSNSシェア機能を実装した。
ポコアプリでは「成果の可視化」がモチベーション維持に不可欠だと考え、過去の学習データを用いたStreaks（連続記録）やTotal Timeを表示するリッチなUIを作成した。

実装のハイライト：
1. **ActivityModal**: 週/月/年で切り替え可能な詳細な統計画面。
2. **ShareCard**: SNS投稿用に、ユーザーの実績（ストリーク、活動ヒートマップ、カテゴリ分布）を一枚の画像として生成する機能。「Growth Track」として直近12日間の活動をヒートマップ表示する独自UIも組み込んだ。
3. **リワード設計**: シェアを行うと24時間広告が非表示になるインセンティブを導入。ユーザー行動（シェア）とアプリの収益ポイント（広告）をうまく連動させる狙い。

UIデザインには特にこだわり、ダークモードを基調としたモダンなデザインに刷新した。
最初はライブラリの問題でうまくいかなかったが、最終的にはドーナツチャートやヒートマップも綺麗に表示できるようになった。

これでユーザーが自分の頑張りをSNSで自慢できるようになった。
バイラル的な広がりを期待したい。

1/5 (続き)
午後は、アプリの「継続利用性」と「ユーザビリティ」を高める重要機能の実装と、品質向上のための修正を集中的に行った。

**1. ルーティーン（曜日指定）機能の実装**
ユーザーからの要望を受け、特定の曜日（例：毎週月・水・金）に繰り返し行うタスクを一括作成する機能を実装した。
- **UI**: 作成モーダルに `WeekdayPicker` を追加。カレンダーアイコンで展開し、直感的に曜日を選べるようにした。
- **Logic**: `dataService.addRoutineTodos` を実装し、選択した曜日に基づいて向こう30日分のタスクを自動生成する仕組みを構築。
- **Group**: 生成されたタスク群は同一の `srsGroupId` で紐付けられ、履歴画面でまとめて管理できるように設計した。

**2. 使用ガイド（Usage Guide）の大幅刷新**
機能が増えた分、ユーザーへの説明責任を果たすためにガイドを再構築した。
- コンテンツを「はじめに」「基本」「効率化」「振り返り」「その他」の5カテゴリに分類。
- コアコンセプト（忘却曲線）から新機能（Sync, Routine, Rewards）までを網羅し、多言語（日/英）で提供。

**3. 履歴（Activity）画面のUX向上**
- **グループ化**: SRSやルーティーンタスクをアコーディオン形式で表示し、大量のタスクで履歴が埋め尽くされないよう整理。
- **ソート修正**: グループ内のタスクが作成順（ランダム）だった問題を、予定日順（時系列）に並ぶようソートロジックを修正。
- **バグ修正**: 一括削除時の確認ダイアログが表示されない（翻訳キーエラー）問題を解消。

**4. UIの洗練とバグ修正**
- **0:00バグの根絶**: 日付指定のみ（時間未指定）のタスクが「0:00」に配置されてしまう問題を、`null` を明示的に送信することでデータベースのデフォルト値を回避し修正。
- **ビジュアル改善**: シェアカードのヒートマップをカレンダーと同じ緑色に統一し、Todo詳細の「開始」ボタンをオレンジにするなど、テーマカラーの整合性を調整。

**総括**
今日は「統計・SNSシェア」から始まり、「ルーティーン機能」「ガイド拡充」「細かいバグ修正」まで、Web版リリースのハードルとなる主要な課題をほぼ全てクリアできた。
特にルーティーン機能と履歴の整理によって、単発タスクだけでなく「習慣」を管理するツールとしての側面が強化された。
Web版の完成度は極めて高い状態にある。

1/6
SEO最適化を完全実装した。

**実装内容:**
1. **robots.txt**: クロール許可設定とサイトマップ参照を動的生成。
2. **sitemap.xml**: 19言語分のページURLを含むサイトマップを動的生成。
3. **多言語メタデータ**: generateMetadata関数により、各ロケールに最適化されたタイトル・説明文を動的生成。
4. **hreflangタグ**: 全19言語間の代替言語リンクを自動設定し、検索エンジンが適切な言語ページを表示できるように。
5. **Open Graph / Twitterカード**: SNSシェア時のリッチプレビュー対応。
6. **構造化データ (JSON-LD)**: Schema.org準拠のWebApplicationスキーマを追加し、Google検索でのリッチスニペット表示に対応。
7. **動的OGP画像**: ImageResponse APIを使用して、各言語に対応したスタイリッシュなOGP画像を自動生成。

**背景**
先日までに多言語対応（19言語）を完了していたが、SEO対策が未実装だった。
検索エンジンの適切なインデックスと、SNSシェア時の見栄えを改善するため、包括的なSEO対策を実施。
これでWeb版は検索流入とSNS拡散の両面から集客できる状態になった。

リリースに向けた技術的な準備は整いつつある。

1/10
別PCへのデータ移行後、開発環境のセットアップを行った。

**発生した問題と解決:**
1. `node_modules` がGitに含まれていないため `npm install` を実行
2. リモートリポジトリに7コミット分の差分があったため `git pull origin main` で同期
3. `@pomarc/shared` の `dist` フォルダがビルドされておらず、Next.jsでモジュール解決エラーが発生 → `npm run build -w @pomarc/shared` で解決
4. `.env.local` が`.gitignore`に含まれており移行されていなかった → Supabaseダッシュボードから環境変数を再取得して設定

**ワークフローの作成:**
今後のデータ移行やクローン時に同じ問題が起きないよう、`.agent/workflows/setup.md` を作成した。
- 環境変数の設定手順
- 依存関係のインストール
- sharedパッケージのビルド
- トラブルシューティングガイド

`/setup` コマンドで手順を確認できるようになった。

**アカウント登録画面の改善:**
Web版の初回ユーザー体験を大幅に改善した。

1. **ウェルカム画面の実装** (`WelcomeScreen.tsx`)
   - 初回アクセス時（データ未作成）にウェルカム画面を表示
   - アプリの特徴4つをカードで紹介
   - フェードイン＆スタガーアニメーション

2. **ソーシャルログインのUI追加**
   - `AuthContext.tsx` に `signInWithProvider` 関数を追加
   - `AuthModal.tsx` にGoogle/GitHubボタンを追加
   - Supabaseダッシュボードでの設定はリリース前に実施予定

3. **フルページ認証画面** (`/auth`)
   - 専用の認証ページを新規作成
   - 左パネル（青）にアプリ特徴、右パネルにフォーム
   - 装飾的な背景ブラー要素を追加

4. **視覚的改善**
   - 全画面にフェードイン/スライドインアニメーション追加
   - シャドウの強化（blue-500/25等のカラーシャドウ）
   - ホバー/アクティブ状態のインタラクション強化

**カテゴリ色変更機能の実装:**
カテゴリに色を設定し、視覚的に識別しやすくする機能を実装した。

1. **機能概要**
   - カテゴリごとにカラーパレット（9色）から色を選択可能
   - Todoリスト、スケジュール、カレンダーに色を反映

2. **実装詳細**
   - **データモデル**: `Category` インターフェースに `color` フィールドを追加
   - **編集UI**: `CategoryEditor` にカラーインジケーターとパレットUIを追加
   - **TodoList**: カテゴリ名の横に色ドットを表示
   - **DaySchedule**: タイムラインバーのボーダーと背景色（透過）にカテゴリ色を適用
   - **CalendarPane**:
     - ヒートマップを「塗りつぶし」から「枠のみ」に変更（視認性向上）
     - 日付セル内のドットにカテゴリ色を反映

3. **発生した問題と解決**
   - `DaySchedule.tsx` で `onDateChange` プロパティが重複定義されビルドエラーが発生 → 重複箇所を削除して修正

### 2026-01-10
- SRSプロファイルの適用が反映されない不具合を修正。既存タスクへのSRSスケジュール一括生成機能を実装。
- カテゴリのデフォルトカラーを青から透明に変更。カラーパレットに「なし」オプションを追加。
- スケジュール画面やカレンダー画面でのカテゴリ色未設定時の表示をニュートラルに調整。
- アクティビティの「分析」タブと「シェアカード」に、カテゴリ別の時間配分を示す円グラフを追加。
- シェアカードの活動履歴（ヒートマップ）を直近14日間分に拡大し、レイアウトを最適化。
- Todo詳細画面に「明日へ移動」ボタンを追加。未完了タスクを翌日に持ち越す機能を実装。
- 使用ガイド（Usage Guide）からコンセプト説明を削除し、フィードバック促進の内容に刷新。
- ActivityModal.tsxの分析用円グラフにおけるTooltipの型エラーを修正。
- コード品質向上: 全てのconsole.log（14箇所）を削除し、本番環境向けにクリーンアップ。
- 共通UIコンポーネント「EmptyState」を新規作成。
- 利用規約 (Terms of Service) および プライバシーポリシー (Privacy Policy) ページを作成。
- 規約・ポリシーページを個人開発者向けの内容に修正（「運営チーム」から「管理者」へ表記変更）。
- 規約ページから特定の条件下で機能しない戻るボタンを修正（ホームへのLinkに変更）。
**Todo機能の強化とSRS連携 (2026-01-10):**
既存のタスク管理機能をさらに使いやすくするための改善を実施した。

1. **SRS後付け適用**:
   これまでは作成時しかSRSを設定できなかったが、詳細画面から後付けでSRSプロファイルを適用できるようにした。
   これにより、「普通のタスクとして作ったけど、やっぱ定期的に復習したい」というニーズに対応できるようになった。
   実装的には、親タスクのIDを`srsGroupId`として、子タスク（復習用）を一括生成する新しいメソッドを`dataService`に追加した。

2. **アクティビティ履歴からの遷移**:
   履歴画面（ActivityModal）で見返しているときに、「このタスクの詳細を見たい/編集したい」と思うことが多かったため、
   タスク名をクリックして詳細画面に飛べるようにした。地味だがUXへの貢献度は高い。

3. **UIの整理**:
   - 使われていなかった「優先度（Priority）」項目を詳細画面から削除し、画面をスッキリさせた。
   - 詳細画面の「開始」ボタンのデザインが作成画面と違っていた（濃いオレンジベタ塗り vs 薄いオレンジ枠）のを、作成画面側のデザインに統一した。
   - `Play` アイコンのサイズも `18` に統一。

細かな改善だが、アプリの手触りが良くなっていると感じる。
Web版リリースに向けて、こうした「使い勝手」の部分を詰め切っていきたい。

1/16
Web版リリースに向けた本番環境の構築と検証を行った。
- `.env.production` を作成し、本番用のURL設定などを記述。
- `npm run build:web` で本番ビルドを作成し、エラーがないことを確認。
- ローカルで本番サーバーを立ち上げ、ブラウザから動作確認を実施。
- ログイン画面やホーム画面が正しく表示されることを確認した。
これでリリース前の技術的な懸念点は大きく解消された。次は実際のデプロイ作業になるだろう。

1/17
オフラインキューとクラウド削除時のコンソールエラーを修正した。

**発生していた問題:**
- オフラインキュー処理時とTodo削除時に `[OfflineQueue] Failed to process item: "xxx" {}` や `Cloud delete error {}` のようなエラーがコンソールに表示されていた。
- エラーオブジェクトが `{}` と表示され、デバッグが困難だった。

**原因と修正:**
1. **エラーシリアライズの問題**: Supabaseの `PostgrestError` オブジェクトは通常のオブジェクトとして `console.error` に渡すと空オブジェクトとして表示される → エラーの `message`, `code`, `details`, `hint` を明示的に展開してログ出力するよう修正。
2. **認証タイミングの問題**: オフラインキュー処理時にセッションが切れている可能性があった → `processOfflineQueue` 関数の冒頭でセッションチェックを追加し、セッションがない場合はキュー処理をスキップ。
3. **dataService.ts**: `JSON.stringify` を使用してエラーオブジェクト全体を詳細に表示するよう変更。

**検証結果:**
- ブラウザで動作確認を実施し、タスク削除が正常に動作することを確認。
- 以前発生していたエラーが発生しなくなったことを確認。

